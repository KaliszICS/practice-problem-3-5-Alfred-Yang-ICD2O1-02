# Instructions  

1.  Create a method called getFirstName() that takes in a string of two or more names.</br>
Return the first name ONLY.</br>
</br>

2. Create a method called getLastName() that takes in a string of two or more names.</br>
Return the last name ONLY.</br>
</br>

3. Create a method called isValidName() that takes in a String.</br>
Return true if the name has has a first Name and Last Name ONLY.</br>
The Names are only valid if they both have at least 2 characters in them.</br>
